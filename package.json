{
  "name": "my-autolink",
  "version": "0.0.2",
  "description": "My customable autolinker for me.",
  "main": "index.js",
  "typings": "index.d.ts",
  "scripts": {
    "test": "mocha --compilers coffee:coffee-script/register",
    "prepublish": "not-in-install && npm run build && npm run test || in-install",
    "build": "tsc -d",
    "clean": "rm ./index.js ./lib/*.js ./index.d.ts ./lib/*.d.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/uhyo/my-autolink"
  },
  "keywords": [
    "autolink"
  ],
  "author": "uhyo",
  "license": "MIT",
  "devDependencies": {
    "coffee-script": "^1.10.0",
    "in-publish": "^2.0.0",
    "mocha": "^2.3.3",
    "typescript": "^1.8.10"
  },
  "dependencies": {
    "escape-html": "^1.0.3",
    "extend": "^3.0.0",
    "ip-regex": "^1.0.3"
  }
}
